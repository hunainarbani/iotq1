fn main() {
    //we use float against each variable because percentage will be in float
    let x: f32= 95.0; 
    let n: f32 = 150.0;
    let p: f32 = x / n * 100.0;

    println!("Student secure {}%",p);
}
